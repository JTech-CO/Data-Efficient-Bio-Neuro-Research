# Small CPU-only runtime. No biological datasets or weights are downloaded.
numpy>=1.26,<3
scipy>=1.11,<2
jsonschema>=4.20,<5
pytest>=8,<10

# Required only for the preserved original GP example and its regression tests.
matplotlib>=3.8,<4
